# Per-host settings for scripts/vpn-health.py. Copy to scripts/vpn-health.env
# (gitignored) so the real container name never lands in the repo or the unit.
# Any variable the script documents works here; the environment wins over this file.
VPN_CONTAINER=vpn-gateway
VPN_FORWARDED_PORT_FILE=/tmp/vpn-gateway/forwarded_port
