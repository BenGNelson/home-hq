# Frontend (placeholder)

The React + Vite + Tailwind shell + dashboard widgets go here in **build step 4–5**.
For now Phase 1 is backend-only (config layer + /api/system). This folder exists
so the repo structure matches the plan.
